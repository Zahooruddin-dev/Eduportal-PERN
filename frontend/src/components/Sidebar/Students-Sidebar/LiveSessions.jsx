import StudentTabShell from './StudentTabShell';

const LiveSessions = () => {
  return (
    <StudentTabShell
      title='Live Sessions'
      description='Join upcoming classes and keep track of session updates in one place.'
    />
  );
};

export default LiveSessions;
